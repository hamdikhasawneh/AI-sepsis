# ============================================================
# Sepsis Prediction Project — Requirements
# ============================================================
# Install with: pip install -r requirements.txt
#
# For PyTorch with CUDA (RTX 4080 local machine):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# For PyTorch CPU only (Colab without GPU):
#   pip install torch torchvision
# ============================================================
 
# ── Core Data Science ────────────────────────────────────────
numpy>=2.0.0
pandas>=2.0.0
scipy>=1.10.0
 
# ── Machine Learning ─────────────────────────────────────────
scikit-learn==1.5.0
lightgbm>=4.0.0
xgboost>=2.0.0
 
# ── Deep Learning ────────────────────────────────────────────
# Install PyTorch separately — see instructions above
torch==2.5.1
# torchvision>=0.15.0
 
# ── Survival Modeling (Phase 2) ───────────────────────────────
pycox>=0.2.3              # DeepSurv, DeepHit
lifelines>=0.27.0         # survival analysis utilities
 
# ── Explainability ───────────────────────────────────────────
shap==0.51.0              # SHAP values for tree models
survshap>=0.1.0           # SurvSHAP(t) for survival models (Phase 2)
 
# ── Visualization ────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
 
# ── Utilities ────────────────────────────────────────────────
joblib>=1.3.0             # model serialization
pathlib2>=2.3.7           # path utilities
tqdm>=4.65.0              # progress bars
zipfile36>=0.1.3          # zip file handling
zipfile36>=0.1.3          # zip file handling
psutil>=5.9.0             # memory monitoring for large file reads
pyarrow>=12.0.0           # fast parquet read/write for intermediate files
 
# ── Notebook ─────────────────────────────────────────────────
ipykernel>=6.0.0
jupyter>=1.0.0